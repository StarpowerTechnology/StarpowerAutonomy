Agent: Trippy
Query: Prefect flow documentation
Date: 20260317_151113
──────────────────────────────────────────────────
[1] Title: Flows - Prefect Docs
[1] URL: https://docs.prefect.io/v3/concepts/flows
[1] Content: Each flow run has its own [state](/v3/concepts/states) lifecycle that is tracked by the Prefect database. [Deployments](/v3/how-to-guides/deployments/create-deployments) exist to enable [arbitrary infrastructures](/v3/concepts/work-pools) and schedules for your flow to run on. Similarly, when invoking a flow run from a [deployment](/v3/how-to-guides/deployments/create-deployments), Prefect will attempt to coerce provided parameters to the parameter schema implied by your flow function’s type signature. When a nested flow run starts, it creates a new [task runner](/v3/concepts/task-runners) for any tasks it contains. The `state_details` field of the nested flow run includes a `parent_task_run_id`. A [state](/v3/concepts/states) is a record of the status of a particular task run or flow run. * If a flow returns a manually created state, it is used as the state of the final flow run. If `state` were returned from the flow function, the run would be marked as `FAILED`. This flow run finish
...[truncated]

[2] Title: flows - Prefect documentation
[2] URL: https://docs.prefect.io/v3/api-ref/python/prefect-flows
[2] Content: import asyncio import  asyncio import datetime import  datetime from prefect import flow, aserve, get_client from  prefect import flow, aserve, get_client async def init(): async  def  init(): await set_concurrency_limit()  await set_concurrency_limit() async def set_concurrency_limit(): async  def  set_concurrency_limit(): async with get_client() as client:  async  with get_client() as client: await client.create_concurrency_limit(tag='dev', concurrency_limit=3)  await client.create_concurrency_limit(tag = 'dev', concurrency_limit = 3)  @flow @flowasync def my_flow(name): async  def  my_flow(name): print(f"hello {name}")  print(f "hello {name} ") async def main(): async  def  main():  # Initialization function  # Initialization function await init()  await init()  # Run once a day  # Run once a day hello_deploy = await my_flow.to_deployment( hello_deploy =  await my_flow.to_deployment( "hello", tags=["dev"], interval=datetime.timedelta(days=1)  "hello", tags =["dev"], interval =dateti
...[truncated]

[3] Title: Schedule flow runs - Prefect Docs
[3] URL: https://docs.prefect.io/v3/concepts/schedules
[3] Content: Prefect can schedule when to automatically create new flow runs. Schedules tell Prefect when and how to create new flow runs. For example, with `day_or` set to `True`, the cron schedule `* * 3 1 2` runs a flow every minute on the third day of the month and on Tuesday (the second day of the week) in January (the first month of the year). For example, the same schedule, when `day_or` is `False`, runs a flow on every minute on the **third Tuesday** in January. When clocks are set back, this results in two runs that *appear* to be scheduled for 1 AM local time, even though they are an hour apart in UTC time.For longer intervals, like a daily schedule, the interval schedule adjusts for DST boundaries so that the clock-hour remains constant. These settings mean that if a deployment has an hourly schedule, the default settings will create runs for the next four days (or 100 hours).

[4] Title: Quickstart - Prefect Docs
[4] URL: https://docs.prefect.io/v3/get-started/quickstart
[4] Content: We’ll tell Prefect to clone `https://github.com/PrefectHQ/quickstart` and invoke `01_getting_started.py:main`. uvx prefect-cloud deploy 01_getting_started.py:main \ uvx prefect-cloud  deploy 01_getting_started.py:main  \ --name my_first_deployment \ --name my_first_deployment  \ --from https://github.com/PrefectHQ/quickstart --from https://github.com/PrefectHQ/quickstart. You’ve just run your first Prefect workflow remotely. Prefect makes this easy with the `prefect-cloud schedule` command.Let’s schedule our workflow to run every day at 8:00 AM with [`cron`](https://en.wikipedia.org/wiki/Cron) syntax. After running the command, your workflow will automatically execute remotely according to the schedule you’ve set.You can view and manage all your scheduled runs in the Prefect UI. If you prefer to run your Prefect server in a Docker container, you can use the following command:.

[5] Title: Getting Started with Prefect Flows | by Kalise Richmond - Medium
[5] URL: https://medium.com/the-prefect-blog/getting-started-with-prefect-flows-98e8881ffaf9
[5] Content: This guide is here to help you step through 7 simple steps to get started running a simple flow using Prefect Cloud and your local machine. * *Agents* run on your local machine and dynamically execute flows when there is a flow scheduled using Prefect Cloud to run. ## Next Up: Running your flow with Prefect Cloud. Before we start running our flow with Prefect Cloud, let’s quickly understand what that means. Running the flow with Prefect Cloud uses what we call the hybrid model. But instead of running the flow using the terminal, we will register the flow to Prefect Cloud and send metadata about the flow to our cloud project. Then we will trigger flow runs from Prefect Cloud and use agents that run on our machine to look at Prefect Cloud. You’ve just run a flow on Prefect Cloud!

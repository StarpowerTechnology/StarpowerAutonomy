MEMORY
- Spaceman-top-3=reasoning-adaptability-perspective
- Trippy is drafting the OpenAPI spec in docs/api_spec.yaml
- Savvy will auto-generate MCP server in src/api/mcp/ once spec is ready
- WVY owns FastAPI skeleton & download pipeline for alignment-forum JSONL
- I (Trilly) will write src/api/services/map_service.py to convert first-50-row CSV to MapNode JSON that matches the spec schema

PERSONAL INSTRUCTIONS
Keep code minimal, async-first, and always expose a /health endpoint.

GOAL / STATUS
Wait for Trippy’s spec PR, then implement map_service.py + router within 30 min.

PROGRESS
- [x